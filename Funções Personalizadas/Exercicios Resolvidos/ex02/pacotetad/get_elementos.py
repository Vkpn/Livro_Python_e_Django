def get_elementos(pilha):
    """
    Função para mostrar todos os itens da pilha.
    :param pilha: recebe a própria pilha como parâmetro.
    :return: retorna a propria pilha caso não esteja vazia.
    """
    if len(pilha) == 0:
        print('A pilha está vazia.')
    else:
        return pilha
